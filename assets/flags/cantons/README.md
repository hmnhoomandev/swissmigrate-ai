# Canton flag assets

Place real canton flag SVGs here using lowercase canton codes:

- `zh.svg`
- `ge.svg`
- `vd.svg`
- `be.svg`

The app already supports all 26 cantons. If a real SVG is missing, it renders a
local generated placeholder badge with the canton code, so there are no broken
external images.
